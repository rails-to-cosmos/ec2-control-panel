apiVersion: 1
datasources:
  # uid is fixed because the provisioned dashboards reference it by uid.
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:2725
    isDefault: true
    editable: false
